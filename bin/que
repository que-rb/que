#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative 'command_line_interface'

$stdout.sync = true

exit_code =
  Que::CommandLineInterface.parse(
    args:   ARGV,
    output: $stdout,
  )

exit(exit_code)
