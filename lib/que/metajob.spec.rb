# frozen_string_literal: true

require 'spec_helper'

describe Que::Metajob do
  describe "<=>" do
    it "should sort appropriately"
  end

  describe "priority_sufficient?" do
    it "should indicate whether the given priority is enough to work the given job"
  end
end
